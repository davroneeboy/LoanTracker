# ASGI server
uvicorn[standard]==0.30.6

# Web framework
fastapi==0.115.5

# Validation & settings
pydantic==2.10.2
pydantic-settings==2.6.1

# CORS (included in FastAPI, explicit for middleware)
# Database (SQLite for dev, optional for production)
sqlalchemy==2.0.36

# HTTP client for tests (optional)
httpx==0.27.2
