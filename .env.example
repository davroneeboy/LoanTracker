# ===========================================
# Безопасность: не коммитить .env в репозиторий.
# Все переменные ниже — только для сервера (никогда NEXT_PUBLIC_ для ключей).
# ===========================================

# Бэкенд API (если используется внешний бэкенд)
GL_API=

# --- Google Sheets (только серверные переменные) ---
# JSON сервисного аккаунта целиком (одной строкой, без переносов).
# Создать: Google Cloud Console → APIs & Services → Credentials → Service Account.
# Скачать JSON и вставить сюда (или хранить в секретах CI/CD).
GOOGLE_SERVICE_ACCOUNT_JSON=

# Список разрешённых ID таблиц через запятую. Если пусто — разрешены все (не рекомендуется в проде).
# ID таблицы — из URL: https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
SPREADSHEET_ALLOWED_IDS=
